name: "Docker"

on:
  push:
    branches: ["main"]
    tags: ["*"]

jobs:
  build:
    uses: famedly/backend-build-workflows/.github/workflows/docker-backend.yml@v2
    secrets: inherit
    with:
      name: hedwig
      oss: true
